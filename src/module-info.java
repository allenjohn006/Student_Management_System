module StudentManagementSystem {
	// This is required for all Java Swing (GUI) components
	requires java.desktop;
	
	// This is required for all JDBC (Database) components
	requires java.sql;
	
	// (All the "requires kernel", "requires io", etc. lines are GONE)
}